# Core
feedparser==6.0.11
httpx==0.27.2
python-dotenv==1.0.1

# LLM / storage
# (httpx covers DeepSeek REST calls; sqlite3 is stdlib)

# Deduplication — optional but strongly recommended
# Remove these two lines if you cannot install torch in your environment;
# the pipeline will fall back to URL-only dedup automatically.
sentence-transformers==3.3.1
numpy==1.26.4
